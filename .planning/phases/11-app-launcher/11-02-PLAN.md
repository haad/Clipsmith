---
phase: 11-app-launcher
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - Clipsmith/Views/AppLaunchViewModel.swift
  - Clipsmith/Views/AppLaunchController.swift
  - Clipsmith/Views/AppLaunchView.swift
  - ClipsmithTests/AppLaunchViewModelTests.swift
  - Clipsmith.xcodeproj/project.pbxproj
autonomous: true
requirements: []
tags: [macos, swift6, swiftui, app-launcher, bezel-pattern]

must_haves:
  truths:
    - "AppLaunchViewModel.recomputeDisplayedApps returns up to 5 recent apps (in recency order) when searchText is empty (D-04)"
    - "AppLaunchViewModel.recomputeDisplayedApps applies FuzzyMatcher.score and adds +0.1 recency boost for apps whose bundleID is in recentBundleIDs (D-05)"
    - "AppLaunchController is a @MainActor final class subclass of NSPanel that opens with .nonactivatingPanel styleMask at init time (D-08)"
    - "AppLaunchController.launchSelected() hides the bezel FIRST, then calls NSWorkspace.shared.openApplication(at:configuration:completionHandler:) with NSWorkspaceOpenConfiguration (D-09)"
    - "AppLaunchController.show() triggers AppScannerService.refresh() asynchronously each open (D-02)"
    - "AppLaunchView renders icon (24×24) + name rows with selection highlight (D-06) and a TextField that becomes first responder on appear (D-03)"
    - "Icon loading runs on Task.detached(priority: .userInitiated) — never on @MainActor during scan"
    - "AppLaunchViewModelTests passes 4/4 real assertions for fuzzy filter, recency boost, empty-query recent apps, and navigation clamping"
  artifacts:
    - path: "Clipsmith/Views/AppLaunchViewModel.swift"
      provides: "@Observable @MainActor AppLaunchViewModel with apps, recentBundleIDs, searchText, selectedIndex, displayedApps, currentApp, navigation methods"
      contains: "final class AppLaunchViewModel"
    - path: "Clipsmith/Views/AppLaunchController.swift"
      provides: "NSPanel subclass (non-activating bezel #4) with show/hide, sendEvent, keyDown, launchSelected"
      contains: "final class AppLaunchController: NSPanel"
    - path: "Clipsmith/Views/AppLaunchView.swift"
      provides: "SwiftUI view with frosted glass, always-focused TextField, icon+name rows"
      contains: "struct AppLaunchView: View"
  key_links:
    - from: "AppLaunchController.show"
      to: "AppScannerService.refresh"
      via: "Task { await appScannerService?.refresh() }"
      pattern: "appScannerService\\?\\.refresh\\(\\)"
    - from: "AppLaunchController.launchSelected"
      to: "NSWorkspace.shared.openApplication"
      via: "NSWorkspaceOpenConfiguration"
      pattern: "openApplication\\(\\s*at:"
    - from: "AppLaunchViewModel.recomputeDisplayedApps"
      to: "FuzzyMatcher.score"
      via: "score + 0.1 recency boost"
      pattern: "FuzzyMatcher\\.score"
    - from: "AppLaunchView.body"
      to: "AppLaunchViewModel.searchText"
      via: "TextField($viewModel.searchText)"
      pattern: "\\$viewModel\\.searchText"
---

<objective>
Wave 2: implement the AppLaunchViewModel + AppLaunchController + AppLaunchView trio (the fourth bezel in the app). Convert the AppLaunchViewModelTests stubs from Plan 01 into real passing assertions.

Purpose: Deliver the user-facing app launcher bezel surface with ranking, navigation, and launch behavior — fully verifiable via unit tests for ranking + navigation. Bezel display itself is verified in Plan 03 via human checkpoint.

Output:
- `AppLaunchViewModel` with displayedApps computation, recency boost, navigation
- `AppLaunchController` (NSPanel subclass, non-activating, no SwiftData)
- `AppLaunchView` (SwiftUI, frosted glass, icon+name rows, always-focused TextField)
- Filled-in AppLaunchViewModelTests (replacing XCTSkip placeholders from Plan 01)
- Icon loading via Task.detached in AppScannerService (extends Plan 01)
- pbxproj entries for the three new view files
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/11-app-launcher/11-CONTEXT.md
@.planning/phases/11-app-launcher/11-RESEARCH.md
@.planning/phases/11-app-launcher/11-PATTERNS.md
@.planning/phases/11-app-launcher/11-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Contracts from Plan 01 that Plan 02 consumes -->

From `Clipsmith/Services/AppScannerService.swift` (created by Plan 01):
- `struct AppEntry: @unchecked Sendable, Identifiable { var id: URL { url }; let name: String; let url: URL; let bundleID: String?; var icon: NSImage? }`
- `@MainActor @Observable final class AppScannerService` with: `private(set) var apps: [AppEntry]`, `private(set) var recentBundleIDs: [String]`, `private(set) var isLoading: Bool`, `func loadInitially() async`, `func refresh() async`, `func recordLaunch(bundleID: String)`

From `Clipsmith/Services/FuzzyMatcher.swift`:
- `static func score(_ candidate: String, query: String) -> Double?` — returns nil if query is not a subsequence; otherwise returns a value in (0.0, 1.0]; empty query → 1.0

From `Clipsmith/Views/PromptBezelController.swift` (analog for controller pattern, see PATTERNS.md lines 96–280):
- NSPanel subclass with `.nonactivatingPanel + .borderless + .fullSizeContentView` styleMask
- `canBecomeKey` returns true, `canBecomeMain` returns false
- `sendEvent` intercepts Escape (53), Return/Enter (36, 76), arrows (125/126/123/124), Page/Home/End (121/116/119/115)
- `keyDown(with:)` dispatches to navigateDown/Up/etc.
- `registerClickOutsideMonitor()` uses `NSEvent.addGlobalMonitorForEvents(matching: .leftMouseDown)`
- `configureAndPresent()` reads `AppSettingsKeys.bezelWidth`/`bezelHeight`/`wraparoundBezel`, calls `centerOnScreen()`, `makeKeyAndOrderFront(nil)`

From `Clipsmith/Views/PromptBezelView.swift` (analog for view pattern, see PATTERNS.md lines 396–525):
- `@Bindable var viewModel: PromptBezelViewModel`
- `@AppStorage(AppSettingsKeys.bezelAlpha) private var bezelAlpha: Double = 0.25`
- `@FocusState private var isSearchFieldFocused: Bool`
- VStack(spacing: 0) layout with header, TextField, list, footer
- Frosted glass background: ZStack of `.ultraThinMaterial` + `Color(NSColor.windowBackgroundColor).opacity(1.0 - bezelAlpha)`
- ScrollViewReader with onChange(of: selectedIndex) auto-scrolling

From `Clipsmith/Views/PromptBezelViewModel.swift` (analog for viewmodel pattern):
- `@Observable @MainActor final class PromptBezelViewModel`
- Navigation methods: `navigateUp()`, `navigateDown()`, `navigateToFirst()`, `navigateToLast()`, `navigateUpTen()`, `navigateDownTen()`, `navigateTo(index: Int)` — all clamping to filtered list bounds, with optional wraparound
- `currentPrompt: PromptInfo?` computed property — returns filtered[selectedIndex] or nil
- `navigationLabel: String` — returns "N of M"

From Plan 01's `Clipsmith/Settings/AppSettingsKeys.swift`:
- `static let appLauncherEnabled = "appLauncherEnabled"`
- `static let recentAppBundleIDs = "recentAppBundleIDs"`
- Existing keys to reuse: `bezelAlpha`, `bezelWidth`, `bezelHeight`, `wraparoundBezel`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement AppLaunchViewModel and fill in AppLaunchViewModelTests</name>
  <files>Clipsmith/Views/AppLaunchViewModel.swift, ClipsmithTests/AppLaunchViewModelTests.swift</files>
  <read_first>
    - Clipsmith/Views/AppLaunchViewModel.swift (new file — confirm absent)
    - ClipsmithTests/AppLaunchViewModelTests.swift (the stub created in Plan 01 — replace its bodies)
    - Clipsmith/Views/PromptBezelViewModel.swift (analog: search/recompute/navigation pattern; navigation methods to copy semantics from lines 174–223)
    - Clipsmith/Services/AppScannerService.swift (AppEntry struct definition)
    - Clipsmith/Services/FuzzyMatcher.swift (score function signature and behavior)
    - .planning/phases/11-app-launcher/11-PATTERNS.md (AppLaunchViewModel section, lines 284–388)
    - .planning/phases/11-app-launcher/11-RESEARCH.md (Pattern 7 — Recency Boost Ranking, lines 336–365)
    - .planning/phases/11-app-launcher/11-CONTEXT.md (D-03, D-04, D-05 — instant search, top-5 recents, +0.1 boost)
  </read_first>
  <behavior>
    AppLaunchViewModel state:
    - `var apps: [AppEntry] = []` — set externally (didSet triggers recomputeDisplayedApps)
    - `var recentBundleIDs: [String] = []` — injected from AppScannerService before each recompute (didSet triggers recomputeDisplayedApps)
    - `var searchText: String = ""` — didSet resets selectedIndex to 0 and recomputes
    - `var selectedIndex: Int = 0`
    - `var wraparoundBezel: Bool = false`
    - `var isLoading: Bool = false` — mirrored from AppScannerService
    - `var isSearchMode: Bool = true` (always true per D-03)
    - `private(set) var displayedApps: [AppEntry] = []`

    Ranking rules (D-04, D-05):
    - When trimmed searchText is empty: displayedApps = first 5 entries from recentBundleIDs mapped back to AppEntry (in recency order, dropping any IDs not present in apps).
    - When trimmed searchText is non-empty: for each app, call `FuzzyMatcher.score(app.name, query: trimmed)`. Drop apps where score is nil. For apps whose bundleID is in Set(recentBundleIDs), add `0.1` to the score. Sort by score descending; tie-break by lowercased name ascending. Return mapped [AppEntry].

    Computed properties:
    - `var currentApp: AppEntry?` — displayedApps[selectedIndex] if in range, else nil
    - `var navigationLabel: String` — "N of M" or "" if displayedApps is empty

    Navigation methods (copy semantics from PromptBezelViewModel.swift lines 176–223): navigateUp/Down (respect wraparoundBezel), navigateToFirst/Last, navigateUpTen/DownTen, navigateTo(index:) — all clamping to displayedApps.count.

    AppLaunchViewModelTests must replace the Plan 01 XCTSkip stubs with real assertions:
    - `testFuzzyFilterReturnsRankedMatches`: given fixture apps [Safari, Calendar, Calculator], set searchText="calc", assert displayedApps contains Calculator and Calendar but NOT Safari, AND Calculator is first (higher fuzzy score than Calendar for query "calc").
    - `testRecencyBoostPromotesRecentApp`: given fixture apps where two apps both match query "te" (Terminal, TextEdit), set recentBundleIDs=["com.apple.TextEdit"], assert TextEdit appears BEFORE Terminal because of the +0.1 boost (and that without the boost — i.e. with recentBundleIDs=[] — the order would be ranked purely by FuzzyMatcher).
    - `testEmptyQueryShowsRecentApps`: given 10 fixture apps and recentBundleIDs=["com.apple.Safari", "com.apple.Terminal", "com.apple.Calendar"], set searchText="", assert displayedApps == [Safari, Terminal, Calendar] (length 3, in recency order). Then test with 7 recents and assert only first 5 appear.
    - `testNavigationClampsToList`: set displayedApps with 3 entries, call navigateDown 10 times, assert selectedIndex == 2 (clamped). Call navigateToFirst, assert 0. Call navigateToLast, assert 2. Call navigateTo(index: 99), assert 2. With empty displayedApps, calling navigate* must not crash and selectedIndex remains 0.
  </behavior>
  <action>
    Create `Clipsmith/Views/AppLaunchViewModel.swift`. Imports: `import Foundation`, `import Observation`. Declare `@Observable @MainActor final class AppLaunchViewModel` with the properties listed in `<behavior>`. Implement `recomputeDisplayedApps()` per the D-04/D-05 rules. Implement `recentApps()` private helper for the empty-query branch. Implement `currentApp`, `navigationLabel` computed properties. Implement navigation methods with semantics matching PromptBezelViewModel.swift lines 176–223 (verbatim except swap `filteredPrompts` → `displayedApps`).

    Tie-break in the non-empty branch: when two apps have the same boosted score, sort by `name.lowercased() < $1.0.name.lowercased()` ascending. Document this tie-break in a doc comment so Plan 03 human-verify expectations are predictable.

    Open `ClipsmithTests/AppLaunchViewModelTests.swift` (the Plan 01 stub) and replace each `throw XCTSkip(...)` body with real assertions matching the four test cases in `<behavior>`. Construct fixture AppEntry values inline with `AppEntry(name:, url: URL(fileURLWithPath:), bundleID:, icon: nil)`. Use `viewModel.apps = [...]` and `viewModel.recentBundleIDs = [...]` to drive recomputation. Assert on `viewModel.displayedApps`.

    Do NOT touch AppScannerService for icon loading in this task — Task 3 of this plan adds icon loading. Do NOT touch AppLaunchController or AppLaunchView in this task.
  </action>
  <verify>
    <automated>xcodebuild test -scheme Clipsmith -destination 'platform=macOS' -only-testing:ClipsmithTests/AppLaunchViewModelTests 2>&1 | tail -30 | grep -E "Test Suite.*passed|Test Suite.*failed|error:"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "final class AppLaunchViewModel" Clipsmith/Views/AppLaunchViewModel.swift` returns 1
    - `grep -c "@Observable" Clipsmith/Views/AppLaunchViewModel.swift` returns 1
    - `grep -c "@MainActor" Clipsmith/Views/AppLaunchViewModel.swift` returns 1
    - `grep -c "func recomputeDisplayedApps" Clipsmith/Views/AppLaunchViewModel.swift` returns 1
    - `grep -c "FuzzyMatcher.score" Clipsmith/Views/AppLaunchViewModel.swift` returns ≥ 1
    - `grep -c "+ 0.1" Clipsmith/Views/AppLaunchViewModel.swift` returns ≥ 1 (the recency boost constant)
    - `grep -c "var currentApp: AppEntry?" Clipsmith/Views/AppLaunchViewModel.swift` returns 1
    - `grep -c "func navigateUp\\|func navigateDown\\|func navigateToFirst\\|func navigateToLast\\|func navigateUpTen\\|func navigateDownTen\\|func navigateTo" Clipsmith/Views/AppLaunchViewModel.swift` returns ≥ 6
    - `grep -c "throw XCTSkip" ClipsmithTests/AppLaunchViewModelTests.swift` returns 0 (all stubs replaced)
    - `grep -cE "XCTAssertEqual|XCTAssertTrue|XCTAssertFalse" ClipsmithTests/AppLaunchViewModelTests.swift` returns ≥ 8 (multiple assertions across the 4 tests)
    - All four AppLaunchViewModelTests pass: `xcodebuild test -scheme Clipsmith -destination 'platform=macOS' -only-testing:ClipsmithTests/AppLaunchViewModelTests 2>&1 | grep -cE "Test Case.*test(Fuzzy|Recency|EmptyQuery|Navigation).*passed"` returns 4
  </acceptance_criteria>
  <done>AppLaunchViewModel.swift exists with ranking + navigation logic. All four AppLaunchViewModelTests pass. (Tests run after Task 4 wires the new files into pbxproj — if any test fails here, the issue is either ranking logic or test fixtures.)</done>
</task>

<task type="auto">
  <name>Task 2: Implement AppLaunchView (SwiftUI)</name>
  <files>Clipsmith/Views/AppLaunchView.swift</files>
  <read_first>
    - Clipsmith/Views/AppLaunchView.swift (new file — confirm absent)
    - Clipsmith/Views/PromptBezelView.swift (analog: VStack layout, frosted glass, ScrollViewReader, ForEach rows; copy structure verbatim)
    - Clipsmith/Views/AppLaunchViewModel.swift (just-created; the @Bindable target)
    - Clipsmith/Services/AppScannerService.swift (AppEntry struct — icon: NSImage? property)
    - .planning/phases/11-app-launcher/11-PATTERNS.md (AppLaunchView section, lines 396–525)
    - .planning/phases/11-app-launcher/11-CONTEXT.md (D-03, D-06 — instant search, icon + name rows)
  </read_first>
  <behavior>
    - Top-level: `struct AppLaunchView: View` with `@Bindable var viewModel: AppLaunchViewModel`, `@AppStorage(AppSettingsKeys.bezelAlpha) private var bezelAlpha: Double = 0.25`, `@FocusState private var isSearchFieldFocused: Bool`.
    - body: VStack(spacing: 0) with header ("App Launcher" caption), Divider, TextField bound to `$viewModel.searchText` with placeholder "Search apps...", Divider, content area (loading / empty / list), Divider, footer (`viewModel.navigationLabel`).
    - Content area: when `viewModel.isLoading && viewModel.apps.isEmpty` show `ProgressView("Scanning apps...")`; when `viewModel.displayedApps.isEmpty` show `Text("No matches").foregroundStyle(.secondary)`; otherwise render appListView.
    - appListView: ScrollViewReader wrapping ScrollView { LazyVStack(spacing: 0) { ForEach(Array(viewModel.displayedApps.enumerated()), id: \.element.id) { ... } } }, with onChange(of: viewModel.selectedIndex) auto-scroll.
    - appRow: HStack(spacing: 10) with icon (24×24, NSImage if loaded else `Image(systemName: "app.dashed")`), `Text(entry.name)` (`.lineLimit(1)`), Spacer. Background highlight: `Color.accentColor.opacity(0.2)` when isSelected, else `.clear`. Tap gesture calls `viewModel.navigateTo(index:)`.
    - Background: ZStack of `RoundedRectangle(cornerRadius: 16).fill(.ultraThinMaterial)` over `RoundedRectangle(cornerRadius: 16).fill(Color(NSColor.windowBackgroundColor).opacity(1.0 - bezelAlpha))`, then `.clipShape(RoundedRectangle(cornerRadius: 16))`.
    - `.onAppear { isSearchFieldFocused = true }` — D-03 requires the field to be focused on bezel open.
    - TextField uses `.textFieldStyle(.plain)` (matches PromptBezelView style).
  </behavior>
  <action>
    Create `Clipsmith/Views/AppLaunchView.swift`. Imports: `import SwiftUI`. Implement struct per `<behavior>`. Copy the frosted glass and ScrollViewReader patterns verbatim from PromptBezelView.swift lines 87–141 (swap `filteredPrompts` → `displayedApps`, `currentPrompt` → `currentApp`, `PromptInfo` → `AppEntry`).

    The appRow private func takes `(entry: AppEntry, index: Int) -> some View`. Inside, check `if let icon = entry.icon` and render `Image(nsImage: icon).resizable().scaledToFit().frame(width: 24, height: 24)`. Else render `Image(systemName: "app.dashed").frame(width: 24, height: 24).foregroundStyle(.secondary)`. Highlight via `.background(isSelected ? Color.accentColor.opacity(0.2) : Color.clear)`.

    The header should NOT include any category-cycling hint (the launcher has no categories). Title text: "App Launcher" in `.font(.caption.bold())`.

    Do NOT add a `.modelContainer` modifier — the launcher has no SwiftData dependency. Do NOT add `@Query`. Do NOT touch AppLaunchController in this task.
  </action>
  <verify>
    <automated>test -f Clipsmith/Views/AppLaunchView.swift && grep -c "struct AppLaunchView: View" Clipsmith/Views/AppLaunchView.swift</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "struct AppLaunchView: View" Clipsmith/Views/AppLaunchView.swift` returns 1
    - `grep -c "@Bindable var viewModel: AppLaunchViewModel" Clipsmith/Views/AppLaunchView.swift` returns 1
    - `grep -c "@FocusState" Clipsmith/Views/AppLaunchView.swift` returns 1
    - `grep -c "isSearchFieldFocused = true" Clipsmith/Views/AppLaunchView.swift` returns 1
    - `grep -c "TextField(\"Search apps" Clipsmith/Views/AppLaunchView.swift` returns 1
    - `grep -c '.frame(width: 24, height: 24)' Clipsmith/Views/AppLaunchView.swift` returns ≥ 2 (both the icon-loaded and placeholder branches)
    - `grep -c "ProgressView(\"Scanning apps" Clipsmith/Views/AppLaunchView.swift` returns 1
    - `grep -c "ScrollViewReader" Clipsmith/Views/AppLaunchView.swift` returns 1
    - `grep -c ".ultraThinMaterial" Clipsmith/Views/AppLaunchView.swift` returns 1
    - `grep -c "@AppStorage(AppSettingsKeys.bezelAlpha)" Clipsmith/Views/AppLaunchView.swift` returns 1
    - `grep -c "@Query\\|.modelContainer" Clipsmith/Views/AppLaunchView.swift` returns 0 (no SwiftData)
  </acceptance_criteria>
  <done>AppLaunchView.swift exists with the SwiftUI surface per the analog. Build verification happens in Task 4.</done>
</task>

<task type="auto">
  <name>Task 3: Implement AppLaunchController (NSPanel subclass) and add icon loading to AppScannerService</name>
  <files>Clipsmith/Views/AppLaunchController.swift, Clipsmith/Services/AppScannerService.swift</files>
  <read_first>
    - Clipsmith/Views/AppLaunchController.swift (new file — confirm absent)
    - Clipsmith/Services/AppScannerService.swift (extend with icon loading)
    - Clipsmith/Views/PromptBezelController.swift (analog — copy structure, drop hold/flags monitor and SwiftData injection)
    - Clipsmith/Views/AppLaunchViewModel.swift (consumed: viewModel.searchText, selectedIndex, currentApp, navigation methods)
    - Clipsmith/Views/AppLaunchView.swift (rendered inside NSHostingView)
    - .planning/phases/11-app-launcher/11-PATTERNS.md (AppLaunchController section, lines 93–280)
    - .planning/phases/11-app-launcher/11-RESEARCH.md (Pattern 3 — Icon Loading Strategy; Pattern 4 — no SwiftData; Pattern 5 — NSWorkspace launch; Pattern 6 — sendEvent intercept; Pitfalls 2, 3, 4)
    - .planning/phases/11-app-launcher/11-CONTEXT.md (D-02, D-08, D-09 — refresh on open, non-activating panel, hide-then-launch)
  </read_first>
  <behavior>
    AppLaunchController:
    - `@MainActor final class AppLaunchController: NSPanel`
    - Stored properties: `let viewModel = AppLaunchViewModel()`, `var appScannerService: AppScannerService?` (set by AppDelegate in Plan 03), `private var globalMonitor: Any?`. NO `isHotkeyHold`, NO `flagsMonitor`, NO `pasteService`, NO `appTracker`, NO `modelContainer`.
    - Single designated `init()` (no convenience inits). Call `super.init(contentRect: NSRect(x: 0, y: 0, width: 400, height: 320), styleMask: [.nonactivatingPanel, .borderless, .fullSizeContentView], backing: .buffered, defer: false)`. Set level/collectionBehavior/isOpaque/backgroundColor/hasShadow/isMovableByWindowBackground/isReleasedWhenClosed exactly as PromptBezelController lines 84–90. Create `let hostingView = NSHostingView(rootView: AppLaunchView(viewModel: viewModel))`, then `hostingView.sizingOptions = []` (Pitfall 4 — required to prevent infinite constraint loop crash), then `contentView = hostingView`.
    - Because `NSPanel` has its own designated init signatures, also implement the `override init(contentRect:styleMask:backing:defer:)` super call that just forwards to super — copy from PromptBezelController lines 50–62 (required for NSPanel subclass to compile).
    - `override var canBecomeKey: Bool { true }`, `override var canBecomeMain: Bool { false }`.
    - `sendEvent(_:)` override: intercept Escape (53) → hide; Return/Enter (36, 76) → launchSelected; arrows + Page/Home/End → keyDown(with:). Do NOT intercept Tab, j, k, or digits — the launcher is always in search mode and these characters must flow to the TextField.
    - `cancelOperation(_:)` override calls `hide()`.
    - `show()`: reset viewModel.selectedIndex = 0, viewModel.searchText = "", kick off `Task { await appScannerService?.refresh() }` (D-02), then call `configureAndPresent()`. After refresh completes, the viewModel.apps must be re-synced from appScannerService.apps and viewModel.recentBundleIDs must be re-synced from appScannerService.recentBundleIDs — handle this by observing or by setting both directly in the Task after refresh.
    - `configureAndPresent()`: read `bezelWidth`/`bezelHeight`/`wraparoundBezel` from UserDefaults, setContentSize if non-zero, alphaValue = 1.0, centerOnScreen(), makeKeyAndOrderFront(nil), registerClickOutsideMonitor(). Also sync viewModel.apps and viewModel.recentBundleIDs from appScannerService BEFORE makeKeyAndOrderFront so the bezel renders with current state immediately.
    - `hide()`: orderOut(nil), removeClickOutsideMonitor(), reset viewModel.selectedIndex = 0 and viewModel.searchText = "".
    - `keyDown(with:)` routes Escape/Return/arrows/Page/Home/End to hide/launchSelected/navigate*. NO j/k/digit handling (launcher search mode).
    - `launchSelected()`: guard `let entry = viewModel.currentApp else { hide(); return }`. Call `hide()` FIRST. Then `if let bundleID = entry.bundleID { appScannerService?.recordLaunch(bundleID: bundleID) }`. Then `let config = NSWorkspaceOpenConfiguration()` and `NSWorkspace.shared.openApplication(at: entry.url, configuration: config, completionHandler: { _, error in if let error { logger.error(...) } })`.
    - `centerOnScreen()` and `registerClickOutsideMonitor()`/`removeClickOutsideMonitor()` private helpers — copy verbatim from PromptBezelController.swift lines 288–332.
    - File-scope logger with category "AppLaunchController".

    AppScannerService icon loading extension (extends Plan 01):
    - After `scanApps()` completes and `self.apps` is assigned, launch a second `Task.detached(priority: .userInitiated)` that iterates apps and calls `NSWorkspace.shared.icon(forFile: url.path)` for each, building a `[(URL, NSImage)]` array. On completion, hop back to MainActor and update `self.apps` by replacing matching entries with icon-loaded versions. This MUST be in `Task.detached`, NOT on the main thread (Pitfall 2).
    - Add a `private func loadIcons(for entries: [AppEntry]) async -> [AppEntry]` nonisolated helper that maps each entry to a new AppEntry with icon loaded. Call this from refresh() after scanApps() completes, replacing apps when ready.
    - Optional optimization: stream icon updates per app (assign apps incrementally so the bezel UI updates as icons load). Not required for this task — batch update after all icons load is acceptable since scan + icon load is under 2s for ~125 apps per RESEARCH.md A2.
  </behavior>
  <action>
    Create `Clipsmith/Views/AppLaunchController.swift`. Imports: `import AppKit`, `import SwiftUI`, `import OSLog`. Declare file-scope logger with category "AppLaunchController" (mirror PromptBezelController lines 6–9).

    Declare `@MainActor final class AppLaunchController: NSPanel` per `<behavior>`. Implement both inits (the NSPanel `override init(contentRect:styleMask:backing:defer:)` forwarder AND the designated `init()` that sets up the panel and contentView). Implement all overrides and helpers per `<behavior>`.

    For show()/configureAndPresent(): the sync flow is "set viewModel.apps from appScannerService.apps (whatever's currently cached) immediately, present, then kick off refresh in a background Task that re-syncs viewModel state on MainActor when complete." This ensures the bezel opens instantly with the cached list (D-02 says refresh is async on open) and updates with fresh results when refresh completes.

    Modify `Clipsmith/Services/AppScannerService.swift` (created in Plan 01) to add icon loading:
    - Add a `nonisolated private func loadIcons(for entries: [AppEntry]) -> [AppEntry]` that maps each entry through `var copy = entry; copy.icon = NSWorkspace.shared.icon(forFile: entry.url.path); return copy`.
    - In `refresh()`, after `self.apps = entries` (the initial nil-icon list), spawn a `Task.detached(priority: .userInitiated) { let withIcons = await self.loadIcons(for: entries); await MainActor.run { self.apps = withIcons } }`.
    - Note: `NSWorkspace` calls on a detached Task are safe because the API is documented as thread-safe and the operation is read-only. Document this in a comment on loadIcons.

    Do NOT add a `paste` flow — the launcher launches apps, it does not paste content. Do NOT inject pasteService or appTracker into AppLaunchController. Do NOT touch AppDelegate, MenuBarView, or Settings views — those are Plan 03 scope.
  </action>
  <verify>
    <automated>test -f Clipsmith/Views/AppLaunchController.swift && grep -c "final class AppLaunchController: NSPanel" Clipsmith/Views/AppLaunchController.swift && grep -c "loadIcons" Clipsmith/Services/AppScannerService.swift</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "@MainActor" Clipsmith/Views/AppLaunchController.swift` returns ≥ 1
    - `grep -c "final class AppLaunchController: NSPanel" Clipsmith/Views/AppLaunchController.swift` returns 1
    - `grep -c "\\.nonactivatingPanel" Clipsmith/Views/AppLaunchController.swift` returns 1 (passed into super.init styleMask, not set afterwards)
    - `grep -c "hostingView.sizingOptions = \\[\\]" Clipsmith/Views/AppLaunchController.swift` returns 1 (Pitfall 4)
    - `grep -c "openApplication(" Clipsmith/Views/AppLaunchController.swift` returns 1
    - `grep -c "NSWorkspaceOpenConfiguration" Clipsmith/Views/AppLaunchController.swift` returns 1
    - `grep -c "appScannerService?.recordLaunch" Clipsmith/Views/AppLaunchController.swift` returns 1
    - `grep -c "appScannerService?.refresh()" Clipsmith/Views/AppLaunchController.swift` returns ≥ 1 (D-02 refresh on open)
    - `grep -c "func launchSelected" Clipsmith/Views/AppLaunchController.swift` returns 1
    - `grep -c "override func sendEvent" Clipsmith/Views/AppLaunchController.swift` returns 1
    - `grep -c "registerClickOutsideMonitor\\|addGlobalMonitorForEvents" Clipsmith/Views/AppLaunchController.swift` returns ≥ 2
    - Order of operations in launchSelected: `hide()` MUST appear BEFORE `openApplication(`: `awk '/func launchSelected/,/^    }$/' Clipsmith/Views/AppLaunchController.swift | awk '/hide\\(\\)/{h=NR} /openApplication\\(/{o=NR} END{exit !(h<o)}'` exits 0
    - `grep -c "Task.detached" Clipsmith/Services/AppScannerService.swift` returns ≥ 2 (one for scan, one for icon load — Pitfall 2)
    - `grep -c "NSWorkspace.shared.icon(forFile:" Clipsmith/Services/AppScannerService.swift` returns 1
    - NO j/k/digit intercept in sendEvent: `grep -cE '"j"|"k"|"0"\\.\\.\\.' Clipsmith/Views/AppLaunchController.swift` returns 0
  </acceptance_criteria>
  <done>AppLaunchController.swift exists with the non-activating bezel pattern. AppScannerService now loads icons asynchronously via Task.detached. Build verification happens in Task 4.</done>
</task>

<task type="auto">
  <name>Task 4: Register Plan 02 files in Xcode project.pbxproj and run full test suite</name>
  <files>Clipsmith.xcodeproj/project.pbxproj</files>
  <read_first>
    - Clipsmith.xcodeproj/project.pbxproj
    - Plan 11-01-SUMMARY.md for PBX IDs already assigned by Plan 01 (so we pick monotonically higher IDs)
  </read_first>
  <behavior>
    Add pbxproj entries for the three new view files (AppLaunchViewModel.swift, AppLaunchView.swift, AppLaunchController.swift). Each needs:
    1. PBXBuildFile entry in `/* Begin PBXBuildFile section */` (use AA prefix)
    2. PBXFileReference entry in `/* Begin PBXFileReference section */` (use AF prefix)
    3. Group children insertion: all three go in the Views group (find by locating existing entries like `AF0054 /* PromptBezelViewModel.swift */`)
    4. Sources build phase insertion in the Clipsmith target

    After edits, full project must build and the AppLaunchViewModelTests must report 4/4 passing tests. Full test suite must not regress (other test suites still green).
  </behavior>
  <action>
    Grep for the highest AA/AF IDs currently in pbxproj (`grep -oE "(AA|AF)[0-9]+" Clipsmith.xcodeproj/project.pbxproj | sort -u | tail -10`) and assign the next three sequential pairs for AppLaunchViewModel.swift, AppLaunchView.swift, AppLaunchController.swift. Avoid colliding with IDs assigned by Plan 01 (read 11-01-SUMMARY.md first).

    Use Edit tool (not heredoc) to insert: (a) one PBXBuildFile line near `AA0053 /* PromptBezelViewModel.swift in Sources */`; (b) one PBXFileReference line near `AF0054 /* PromptBezelViewModel.swift */`; (c) group child line in the Views group children array; (d) Sources phase entry in the Clipsmith target.

    Run `xcodebuild build -scheme Clipsmith -destination 'platform=macOS'` and assert BUILD SUCCEEDED. Then run `xcodebuild test -scheme Clipsmith -destination 'platform=macOS' -only-testing:ClipsmithTests/AppLaunchViewModelTests -only-testing:ClipsmithTests/AppScannerServiceTests` and assert all tests pass. Then run the full suite to confirm no regressions: `xcodebuild test -scheme Clipsmith -destination 'platform=macOS'`.

    Do NOT register AppLaunchController/ViewModel/View in any test target — they belong only to the main Clipsmith target. Tests access them via `@testable import Clipsmith`.
  </action>
  <verify>
    <automated>xcodebuild build -scheme Clipsmith -destination 'platform=macOS' 2>&1 | tail -20 | grep -E "BUILD (SUCCEEDED|FAILED)" && xcodebuild test -scheme Clipsmith -destination 'platform=macOS' -only-testing:ClipsmithTests/AppLaunchViewModelTests -only-testing:ClipsmithTests/AppScannerServiceTests 2>&1 | tail -40 | grep -E "Test Suite.*passed|Test Suite.*failed|error:"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "AppLaunchViewModel.swift" Clipsmith.xcodeproj/project.pbxproj` returns ≥ 3
    - `grep -c "AppLaunchView.swift" Clipsmith.xcodeproj/project.pbxproj` returns ≥ 3
    - `grep -c "AppLaunchController.swift" Clipsmith.xcodeproj/project.pbxproj` returns ≥ 3
    - `xcodebuild build -scheme Clipsmith -destination 'platform=macOS' 2>&1 | grep -c "BUILD SUCCEEDED"` returns 1
    - `xcodebuild test -scheme Clipsmith -destination 'platform=macOS' -only-testing:ClipsmithTests/AppLaunchViewModelTests 2>&1 | grep -c "Test Suite 'AppLaunchViewModelTests' passed"` returns 1
    - All four AppLaunchViewModelTests pass: `xcodebuild test ... -only-testing:ClipsmithTests/AppLaunchViewModelTests 2>&1 | grep -cE "Test Case.*test(Fuzzy|Recency|EmptyQuery|Navigation).*passed"` returns 4
    - Full suite still passes: `xcodebuild test -scheme Clipsmith -destination 'platform=macOS' 2>&1 | grep -c "TEST SUCCEEDED"` returns 1
  </acceptance_criteria>
  <done>All three new view files compile under the Clipsmith target. AppLaunchViewModelTests passes 4/4. Full test suite remains green. The trio (Controller/ViewModel/View) is ready for Plan 03 to wire into AppDelegate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AppLaunchView.searchText → AppLaunchViewModel.recomputeDisplayedApps | User-typed search; consumed for display filter only, never passed to FileManager/NSWorkspace |
| AppLaunchViewModel.currentApp.url → NSWorkspace.openApplication | URL originated from AppScannerService scan of whitelisted paths only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02 | Elevation of privilege | AppLaunchController.launchSelected | mitigate | The URL passed to `openApplication(at:)` is `viewModel.currentApp.url`, which originates from `AppScannerService.scanApps()` enumeration of whitelisted paths. The search query is filter-only (string comparison via FuzzyMatcher) and never reaches NSWorkspace. Verified by code path: `searchText → recomputeDisplayedApps → displayedApps (filtered AppEntry list) → currentApp.url`. |
</threat_model>

<verification>
- AppLaunchViewModel + Controller + View all compile under the Clipsmith target
- AppLaunchViewModelTests: 4/4 passing
- AppScannerServiceTests: 4/4 still passing (from Plan 01)
- Full test suite: TEST SUCCEEDED
- launchSelected order verified by static analysis: hide() called BEFORE openApplication()
- Icon loading is off-main-thread (Task.detached in AppScannerService.refresh)
- No SwiftData references in AppLaunchController/View
</verification>

<success_criteria>
- Three new view files exist, compile, and are wired into pbxproj
- AppScannerService now loads icons asynchronously
- AppLaunchViewModelTests passes 4/4 with real assertions (not skipped)
- Full Clipsmith test suite is green
- Phase 11 surface (the bezel itself) is implemented but not yet wired — Plan 03 handles hotkey registration, menu bar entry, settings toggle, and human-verify
</success_criteria>

<output>
Create `.planning/phases/11-app-launcher/11-02-SUMMARY.md` when done, listing: PBX IDs assigned, test counts, ranking edge cases discovered during implementation, and any notes for Plan 03 (e.g. expected behavior when AppScannerService hasn't completed initial scan at hotkey-press time).
</output>
