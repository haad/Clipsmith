---
phase: 06-quick-actions-performance
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - FlycutSwift/Services/TextTransformer.swift
  - FlycutSwift/Services/ClipboardExportService.swift
  - FlycutSwift/Services/ClipboardStore.swift
  - FlycutTests/TextTransformerTests.swift
  - FlycutTests/ClipboardExportServiceTests.swift
autonomous: true
requirements: [QACT-01, QACT-02, PERF-01]

must_haves:
  truths:
    - "TextTransformer.uppercase/lowercase/titleCase/trim/urlEncode/urlDecode produce correct output for typical clipboard text"
    - "TextTransformer.wrapInQuotes/markdownCodeBlock/jsonPrettyPrint produce correct formatted output; invalid JSON input is returned unchanged"
    - "ClipboardExportService encodes all clippings to valid JSON with ISO 8601 timestamps"
    - "ClipboardExportService import round-trips: export then import restores all records with original timestamps"
    - "ClipboardStore.insert accepts an optional timestamp parameter for import use"
  artifacts:
    - path: "FlycutSwift/Services/TextTransformer.swift"
      provides: "Pure text transform and format functions"
      contains: "enum TextTransformer"
    - path: "FlycutSwift/Services/ClipboardExportService.swift"
      provides: "JSON export/import for clipboard history"
      contains: "ClipboardExportService"
    - path: "FlycutTests/TextTransformerTests.swift"
      provides: "Unit tests for all transforms and formats"
      min_lines: 80
    - path: "FlycutTests/ClipboardExportServiceTests.swift"
      provides: "Round-trip export/import tests"
      min_lines: 40
  key_links:
    - from: "FlycutSwift/Services/ClipboardExportService.swift"
      to: "FlycutSwift/Services/ClipboardStore.swift"
      via: "fetchAll + content/sourceAppName/sourceAppBundleURL/timestamp accessors"
      pattern: "store\\.fetchAll|store\\.content|store\\.insert"
---

<objective>
Create the pure-logic foundations for Phase 6: a TextTransformer enum with all text transform and format functions, and a ClipboardExportService for history export/import. Both are pure-Swift types with no UI dependencies, ideal for TDD.

Purpose: These are the building blocks that Plan 02 (quick action menu) and Plan 03 (export/import UI + adaptive polling) will consume. Implementing them first with full test coverage ensures correctness before wiring into the UI.

Output: Two new source files with tests, plus a minor additive change to ClipboardStore.insert() to accept an optional timestamp parameter for import.
</objective>

<execution_context>
@/Users/haad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/haad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-quick-actions-performance/06-RESEARCH.md

@FlycutSwift/Services/ClipboardStore.swift
@FlycutTests/TestModelContainer.swift
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From FlycutSwift/Services/ClipboardStore.swift:
```swift
@ModelActor
actor ClipboardStore {
    func insert(content: String, sourceAppName: String? = nil, sourceAppBundleURL: String? = nil, rememberNum: Int) throws
    func fetchAll(limit: Int? = nil) throws -> [PersistentIdentifier]
    func content(for id: PersistentIdentifier) -> String?
    func sourceAppName(for id: PersistentIdentifier) -> String?
    func sourceAppBundleURL(for id: PersistentIdentifier) -> String?
    func timestamp(for id: PersistentIdentifier) -> Date?
    func clearAll() throws
}
```

From FlycutSwift/Models/Schema/FlycutSchemaV1.swift:
```swift
@Model final class Clipping {
    var content: String
    var timestamp: Date
    var sourceAppName: String?
    var sourceAppBundleURL: String?
    init(content: String, sourceAppName: String? = nil, sourceAppBundleURL: String? = nil, timestamp: Date = .now)
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TextTransformer enum with TDD</name>
  <files>FlycutSwift/Services/TextTransformer.swift, FlycutTests/TextTransformerTests.swift</files>
  <behavior>
    - uppercase("hello world") == "HELLO WORLD"
    - lowercase("HELLO WORLD") == "hello world"
    - titleCase("hello world") == "Hello World"
    - trimWhitespace("  hello  \n") == "hello"
    - urlEncode("hello world & more") contains "%20" and returns non-nil
    - urlDecode("hello%20world") == "hello world"
    - urlDecode on non-encoded string returns original
    - wrapInQuotes("hello") == "\"hello\""
    - markdownCodeBlock("code") == "```\ncode\n```"
    - jsonPrettyPrint valid JSON produces indented output
    - jsonPrettyPrint invalid JSON returns original string unchanged
    - copyAsRTF("hello") returns non-nil Data (RTF bytes)
  </behavior>
  <action>
    RED: Create FlycutTests/TextTransformerTests.swift with XCTest cases for all behaviors listed above. Import XCTest and @testable import FlycutSwift. Create test methods: testUppercase, testLowercase, testTitleCase, testTrimWhitespace, testUrlEncode, testUrlDecode, testWrapInQuotes, testMarkdownCodeBlock, testJsonPrettyPrintValid, testJsonPrettyPrintInvalid, testCopyAsRTFProducesData. Run tests -- they must FAIL (TextTransformer doesn't exist yet).

    GREEN: Create FlycutSwift/Services/TextTransformer.swift as a no-case enum with static functions:
    - uppercase(_:) -> String: s.uppercased()
    - lowercase(_:) -> String: s.lowercased()
    - titleCase(_:) -> String: s.capitalized (accept apostrophe edge case per RESEARCH.md Pitfall 6)
    - trimWhitespace(_:) -> String: s.trimmingCharacters(in: .whitespacesAndNewlines)
    - urlEncode(_:) -> String: s.addingPercentEncoding(withAllowedCharacters: .urlQueryAllowed) ?? s
    - urlDecode(_:) -> String: s.removingPercentEncoding ?? s
    - wrapInQuotes(_:) -> String: "\"\(s)\""
    - markdownCodeBlock(_:) -> String: "```\n\(s)\n```"
    - jsonPrettyPrint(_:) -> String: JSONSerialization guard pattern (return original if not valid JSON)
    - copyAsRTF(_:) -> Data?: NSAttributedString(string:attributes:).rtf(from:documentAttributes:) with monospaced system font

    All functions are static and nonisolated by default -- no Swift 6 actor isolation issues.
    Run tests -- they must PASS.

    REFACTOR: Verify no cleanup needed (functions are one-liners).
  </action>
  <verify>
    <automated>xcodebuild test -scheme FlycutSwift -destination 'platform=macOS' -only-testing FlycutTests/TextTransformerTests 2>&1 | tail -5</automated>
  </verify>
  <done>All 11+ TextTransformer tests pass. TextTransformer enum exists with all transform/format/RTF functions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ClipboardExportService + ClipboardStore timestamp param with TDD</name>
  <files>FlycutSwift/Services/ClipboardExportService.swift, FlycutSwift/Services/ClipboardStore.swift, FlycutTests/ClipboardExportServiceTests.swift</files>
  <behavior>
    - Export of empty history produces valid JSON with version=1 and empty clippings array
    - Export of 3 clippings produces JSON with 3 records, each with content/timestamp/sourceAppName
    - Import of exported JSON round-trips: all content, timestamps, and sourceAppName values are preserved
    - Import skips records whose content already exists in history (prevents duplicates)
    - ClipboardStore.insert with explicit timestamp uses that timestamp instead of .now
  </behavior>
  <action>
    STEP 1 (additive, no test needed): Add `timestamp: Date = .now` parameter to ClipboardStore.insert(). In the non-dedup branch, pass it to Clipping init: `FlycutSchemaV1.Clipping(content:, sourceAppName:, sourceAppBundleURL:, timestamp: timestamp)`. In the dedup branch (when moving to top), continue using `.now` as before. This is backward-compatible -- all existing callers use the default.

    RED: Create FlycutTests/ClipboardExportServiceTests.swift. Use @MainActor class and in-memory SwiftData container (same pattern as ClipboardStoreTests/SnippetStoreTests). Test methods:
    - testExportEmptyHistory: export with no clippings -> valid JSON, version=1, clippings=[]
    - testExportWithClippings: insert 3 clippings, export -> 3 records with correct content
    - testImportRoundTrip: insert 3 clippings, export to Data, clearAll, import from Data -> 3 records restored with original timestamps
    - testImportSkipsDuplicates: insert 2 clippings, export, import same data -> still only 2 clippings (no duplicates)
    Run tests -- they must FAIL.

    GREEN: Create FlycutSwift/Services/ClipboardExportService.swift:
    - Define ClippingExport Codable struct with version:Int, exportedAt:Date, clippings:[ClippingRecord]
    - Define ClippingRecord Codable struct with content:String, sourceAppName:String?, sourceAppBundleURL:String?, timestamp:Date
    - exportHistory(from store: ClipboardStore) async throws -> Data: fetch all IDs, read content/sourceAppName/sourceAppBundleURL/timestamp for each, encode with JSONEncoder (prettyPrinted, sortedKeys, iso8601 dates)
    - importHistory(into store: ClipboardStore, from data: Data, merge: Bool) async throws -> Int: decode JSON, if !merge call store.clearAll() first, then for each record call store.insert(content:, sourceAppName:, sourceAppBundleURL:, timestamp:, rememberNum: Int.max) -- use Int.max to avoid trimming during import. Skip records whose content matches existing (fetch existing contents first for O(1) lookup). Return count of imported records.

    Note: ClipboardExportService should be a simple enum (no-case namespace) with static async functions, not an actor or class. The functions take ClipboardStore as a parameter.

    Run tests -- they must PASS.
  </action>
  <verify>
    <automated>xcodebuild test -scheme FlycutSwift -destination 'platform=macOS' -only-testing FlycutTests/ClipboardExportServiceTests 2>&1 | tail -5</automated>
  </verify>
  <done>ClipboardExportService exports valid JSON and imports round-trip correctly. ClipboardStore.insert accepts optional timestamp. All 4+ tests pass.</done>
</task>

</tasks>

<verification>
Run all new tests together:
```
xcodebuild test -scheme FlycutSwift -destination 'platform=macOS' -only-testing FlycutTests/TextTransformerTests -only-testing FlycutTests/ClipboardExportServiceTests
```

Run full test suite to confirm no regressions:
```
xcodebuild test -scheme FlycutSwift -destination 'platform=macOS'
```
</verification>

<success_criteria>
- TextTransformer enum exists with 10 static transform/format functions + 1 RTF function
- ClipboardExportService exports all clippings to valid JSON and imports them back
- ClipboardStore.insert() accepts optional timestamp parameter (backward-compatible)
- All new tests pass (15+ test methods)
- Full test suite passes (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/06-quick-actions-performance/06-01-SUMMARY.md`
</output>
